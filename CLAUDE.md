# CLAUDE.md — Pályázatradar

## Mi ez

Napi pályázatfigyelő Attila közösségi-kulturális központ projektjéhez (kft + egyesület).
GitHub Actions futtatja naponta; új kiírásnál Issue-t nyit.

- **Repo:** https://github.com/nemelvagyunk/palyazatradar (PUBLIKUS, fiók: `nemelvagyunk`)
- **Lokális mappa:** `C:\Users\B650\Claude\Projects\Ovi kultúrális központ\palyazatradar-github` — ez a git repo gyökere, origin a fenti repo
- Élesítve: 2026-07-22. Első futás OK (alapállapot: 10 forrás, 2922 tétel, Issue #1).

## Architektúra

| Fájl | Szerep |
|------|--------|
| `radar.py` | Minden logika egyben: `FORRASOK` (14 forrás), letöltés (requests), linkkigyűjtés (BeautifulSoup), `KULCSSZAVAK`-szűrés, URL-normalizálás, NKA-/RSS-/Józsefváros-/közlemény-speciális kezelés, forrásonkénti csendes alapállapot-felvétel, bulk-guard, tétel-dúsítás (megjelenés + határidő), `UJ_HATAR` cutoff, watch-oldalak (hash+diff), allapot-diff, `report.md` írás, `GITHUB_OUTPUT`-ba `new_count` + `first_run` + `baselined` + `changes` |
| `.github/workflows/radar.yml` | Cron: `30 5 * * *` (UTC!) + kézi `workflow_dispatch`; jogok: `contents: write`, `issues: write`; lépések: futtatás → allapot.json commit/push → Issue nyitás `gh issue create`-tel |
| `allapot.json` | Kulcs: normalizált URL, `nka-kollegium:<név>` vagy `forras-alap:<forrásnév>` (utóbbi: a forrás alapállapota már felvéve); érték: első észlelés dátuma. **A bot commitolja naponta** — kézzel ne szerkeszd, munka előtt mindig `git pull`! |
| `docs/adatok.json` | A weboldal adatfájlja: `{frissitve, tetelek: {kulcs: {cim, forras, kinek, elso, utolso, megjelent?, hatarido?, palyazhat?, dusitva?}}}`. `palyazhat`: a kiírás szövegéből kiolvasott jogosultak (civil/vallalkozas/onkormanyzat/maganszemely); `dusitva`: mikor néztük meg az oldalát. **Ezt is a bot commitolja naponta.** |
| `oldalak.json` + `data/pages/` | A watch-oldalak hash-állapota + normalizált szövegcache (a diff-kivonathoz). **A bot commitolja.** |
| `docs/index.html` | Publikus weboldal (GitHub Pages, main ág `/docs`): kereshető/szűrhető lista — https://nemelvagyunk.github.io/palyazatradar/ |
| `report.md` | Futásonkénti riport, gitignore-olva |

## Fejlesztési workflow

1. `git pull` (a bot naponta commitol az `allapot.json`-ba!)
2. Módosítás a `radar.py`-ban / `radar.yml`-ben
3. Helyi teszt: `python radar.py --state teszt_allapot.json --report teszt_report.md --adatok teszt_adatok.json --oldalak teszt_oldalak.json --cache teszt_pages` — **SOHA ne az éles állapotfájlokkal tesztelj** (elnyelné az új találatokat / felülírná az éles cache-t); a tesztfájlokat ne commitold
4. Commit + push → kézi ellenőrzés: GitHub → Actions → Pályázatradar → Run workflow
5. Push GitHub Desktopból is mehet (a "palyazatradar" repo be van állítva benne); CLI push először hitelesítést kérhet

## Fontos tudnivalók / gotchák

- **A repo publikus** — érzékeny adat (üzleti fájlok, kulcsok, tokenek) soha ne kerüljön bele.
- **A szülőmappa ("Ovi kultúrális központ") NEM git repo, és ne is legyen az.** 2026-07-22-én incidens volt: az egész projektmappa (könyvelés, pénzügyi modell) véletlenül felment egy publikus "radar1" repóba — törölve lett, a gyökér `.git` eltávolítva. Ne ismételjük meg.
- `palyazat.gov.hu`: a HTML JS-alapú, géppel olvashatatlan. Van hivatalos RSS-e (`/rss.xml`, statikus XML), **de az oldal geo-blokkolja a külföldi IP-ket** — GitHub-hostolt runnerről `Connection refused` (2026-07-22-én kétszer igazolva; magyar IP-ről HTTP 200). **Ne próbáld újra Actions-ből** — helyette a `palyazatok.org` kategóriák a proxy (kkv + civil + kulturális/művészeti). A `rss_tetelek()` feldolgozó + `RSS_KIZARAS` a kódban maradt (`special: "rss"`), más feedhez újrahasznosítható.
- **`palyazat.gov.hu` közlemények — JSON API (2026-08-12):** a `/kozlemenyek` oldal Next.js-es, a lista NINCS a nyers HTML-ben (BeautifulSoup-pal használhatatlan), de mögötte nyílt, kulcs nélküli JSON API van: `GET https://ginapp-api.fair.gov.hu/api/announcements?limit=100&skip=0` → `{result: [{title, dateOfPublication, lead, announcementTypeCode, urlAlias}], meta: {totalCount, skip, limit}}`, dátum szerint csökkenő sorrendben (összesen ~21 400 közlemény). Cikk-URL: `https://www.palyazat.gov.hu/kozlemenyek/<urlAlias>`. Forrásopciók: `special: "kozlemeny"` → `kozlemeny_tetelek()`, ami lapoz, amíg egy oldal legrégebbi tétele a `KOZLEMENY_HATAR` alá nem esik. Szűrés: `KOZLEMENY_TIPUS = "megjelenes"` (csak új kiírás; a „Módosulás" és a „Rendszerinformációk" a zaj nagy része — a friss 50-ből 18 + 13) és `KOZLEMENY_HATAR = "2026-04-12"`. **A `lead` mező tartalmazza a közlemény szövegét**, ezért a dúsítás ebből megy (`tetel_dusitas(lead)` → határidő + jogosultság), a cikkoldalt NEM töltjük le — és a tétel rögtön `dusitva` jelölést kap, hogy a háttér-dúsítás se próbálja (különben 10 geo-blokkolt hiba leállítaná az egészet). **ELDŐLT (2026-08-12, éles futás): a runner ELÉRI.** A geo-blokk csak a `palyazat.gov.hu`-ra vonatkozik, a `ginapp-api.fair.gov.hu`-ra nem — az első futás 19 találatot hozott (pont annyit, amennyi „megjelenés" típusú közlemény 2026-04-12 óta van). Vagyis a palyazat.gov.hu tartalma **mégis elérhető Actions-ből**, csak az API-hoszton keresztül. Ha később más gov.hu-s forrás kell, érdemes ugyanígy az XHR-hívásokat megnézni a böngésző fejlesztői eszközeiben, nem a HTML-t scrapelni.
- `pafi.hu`: aggregátor, minden `/p/` útvonalú link pályázat, de a címben gyakran nincs kulcsszó → forrás-opciók: `utvonal_elotag: "/p/"` + `kulcsszo_nelkul: True` (a kulcsszó-szűrés kikapcsolva, az előtag szűr). Az első 3 listaoldalt figyeljük (legfrissebbek elöl).
- **`jozsefvaros.hu` (2026-08-12-én lecserélve):** a régi `/palyazatok` listaoldal a TELJES hirdetőtáblát adta vissza — 2681 tétel, jórészt állásajánlat (takarító, biztonsági őr), garázsbérlet, ingatlan —, és mivel a linkek szövege mind „További részletek", a weboldalon minden cím ez volt. Új forrás: `/otthon/hirdetotabla/palyazatok/palyazatok-szervezeteknek-tarsashazaknak-maganszemelyeknek/`, saját parserrel (`special: "jozsefvaros"` → `jozsefvaros_tetelek()`). Az oldal Bootstrap-accordion: minden `div.accordion-item` fejlécében a `.s_title` három közvetlen gyereke `[sorszám, határidő, cím]`, a testében a `/otthon/hirdetotabla/palyazat/` link → **a címet ÉS a határidőt is a listáról kapjuk, dúsítás nélkül** (a `LISTA_HATARIDOK` dict viszi át a `main()`-be, és felülírja a dúsított értéket). Az oldal 2014-ig visszamenő teljes archívum (~780 tétel), ezért `JOZSEFVAROS_VISSZA_NAP = 90`: csak az élő és a legfeljebb 90 napja lejárt kiírás kerül be; a határidő nélküli („folyamatos") mindig. A 2681 régi tétel az `allapot.json`-ból és a `docs/adatok.json`-ból törölve, a `forras-alap:Józsefváros` marker is → a forrás a következő futáskor csendben újra alapoz.
- **Forrás-átvizsgálás (2026-08-12) — a 14 forrásból 12 lett, egyenként átbeszélve:**
  1. **Norvég Civil Alap** – `eeagrants.org/…/calls` + `/news` (útvonal-szabály) **és** `palyazat.norvegcivilalap.hu` (kulcsszó). Ehhez az `utvonal_elotag` mostantól lehet **dict** is, hoszt→előtag leképezéssel (a hoszt-egyezés PONTOS, +`www.`; a végződés-vizsgálat ráült volna az `einfoszab.budapest.hu`-ra).
  2. **Hangfoglaló** – változatlan.
  3. **NKA** – a gyűjtő listaoldal helyett a **hírkategória** (útvonal + kulcsszó: 24 hírből 3) **és 10 kollégiumi aloldal**; `nka_tetelek()` a bejövő URL alapján ágazik el. A kollégiumi oldalon a felhívás magán a lapon van („Jelenleg nincs elérhető pályázati felhívás." = nincs találat), a cím `<Kollégium> – <első félkövér sor>`. A relatív felhívás-linket `_nka_kanonikus()` egységesíti (az oldal `…/kollegiumok-felhivasai/<kollégium>/<felhívás>`-ról a rövidebb alakra irányít át → különben duplikálódna).
  4. **Bethlen Gábor Alapkezelő (NEA, Városi Civil Alap)** – két alapoldal; a jobb oldali „AKTUÁLIS" doboz ugyanabban a HTML-ben van, külön kezelés nélkül bejön. Új `csak_gyoker` opció: csak az **egy szegmensű, kötőjeles** slug (a valódi cikkek), így az ÁSZF/GYIK/útmutató `/tamogatasok/…` fa és az egyszavas nyitólapok kiesnek. A Városi Civil Alap emiatt lekerült a watch-listáról.
  5. **Erasmus+ / ESC** – plusz a `/erasmus_hirek` hírfolyam (10 hírből 1 megy át a kulcsszó-szűrésen).
  6. **Visegrádi Alap** – URL változatlan; a javítás a dátumkezelésben (lásd lentebb).
  7. **Kreatív Európa** – új `csak_sajat_domain` opció: a 3 zavaró tétel mind külső hivatkozás volt (ec.europa.eu). Szándékosan NEM `/palyazatok/` előtag, hogy a más útvonalon megjelenő kiírás ne vesszen el.
  8. **palyazatok.org** – a korábbi HÁROM forrás (KKV, Civil, Kulturális/művészeti) egybeolvadt, és a négy kategória-aloldal helyett a **főoldal „Friss pályázatok" karusszelje** (`palyazatok_org_tetelek()`). Kattintgatni nem kell: a slick mind a 10 diát beleírja a kiszolgált HTML-be, a `slick-cloned` másolatokat a böngésző teszi hozzá. A cím a dia `<h4>`-jéből jön (a kép linkje üres szövegű). A második karusszel (`.posts-slider`) referenciacikk, kihagyva.
  9. **palyazat.gov.hu közlemények** – JSON API (lásd fentebb).
  10. **Budapest Főváros** – `budapest.hu/hirek` (útvonal `/hirek/20`, cím az `aria-label`-ből, mert a linkszöveg mindenhol „(új ablakban nyílik meg)") **és** `einfoszab.budapest.hu` közzétételi **táblázat** (`budapest_tetelek()`): cím | benyújtás helye | **határidő** | **közzététel** — dúsítás nélkül kész adat; kulcs a „Részletek" sorazonosítós linkje.
  11. **Józsefváros** – lásd fentebb.
  12. **PAFI** – változatlan.
- **Sablonos linkszöveg (2026-08-12):** `GENERIKUS_LINKSZOVEG` — ha a link szövege „(új ablakban nyílik meg)", „Tovább", „Bővebben", „Részletek" stb., a cím az `aria-label` / `title` / `img alt` attribútumból jön, és csak végső esetben a slugból.
- **Angol dátum + szintezett határidő-kulcsszavak (2026-08-12):** a `DATUM_ANGOL` felismeri az „Oct 1, 2026" alakot (eddig egyetlen Visegrád-tételnek sem volt határideje!). A `hatarido_kinyerese()` három szinten keres: **1.** *Final application deadline* / beadási-benyújtási határidő, **2.** *application/submission deadline*, jelentkezési/pályázati határidő, **3.** a régi általános szavak. Így a visegradfund.org „Timeline" blokkjában a *Final application deadline* veri a *Call opens*, a *Draft submission deadline* és a *Results announced* sorát.
- **„Tényleg pályázat-e?" tartalom-ellenőrzés (2026-08-12):** a `tartalom_ellenorzes: True` forrásoknál (budapest.hu/hirek) a letöltött cikk SZÖVEGE dönt: `palyazat_e_szoveg()` — egy ERŐS jel elég (*pályázati felhívás*, *pályázók köre*, *benyújtási határidő*, *pályázatot hirdet*…), gyengéből kettő kell. Így a „a főváros sikeresen pályázott" típusú hír nem minősül kiírásnak. Ami kiesik, `nem-palyazat:<url>` kulccsal tiltólistára kerül (mint a munkaajánlatok) — többé le sem töltjük.
- **Erasmus+ / ESC kivezetve (2026-08-12):** a teljes állományában egyetlen, a projekthez releváns kiírás sem volt — csak intézményi mobilitási hírek. A forrás kikerült a `FORRASOK`-ból, és felkerült a `MEGSZUNT_FORRASOK` halmazba: az itt szereplő nevekhez tartozó tételeket minden futás kitakarítja az `adatok.json`-ból, különben örökre bent ragadnának „már nem listázott" jelöléssel. **Forrás átnevezésekor NE ide vedd fel** — az átnevezett forrás tételei a legközelebbi észleléskor úgyis megkapják az új nevet.
- **Eredményhirdetések kiszűrése (2026-08-12):** `eredmenyhirdetes(cim, url)` — a hírfolyamokban rengeteg a *már lezajlott* pályázat kimenete („Lezárult az első három felhívás, eredményhirdetés júliusban", „Megjelentek a … eredményei", „Bemutatkoznak a … támogatottjai", „Döntés született…"). A CÍM és az URL-slug alapján is illeszt (a slug ékezet nélküli → minden mintának van ékezetmentes párja). Mind a négy gyűjtő hívja, ÉS minden futáskor kitakarítja a MEGLÉVŐ állományból is (`eredmeny_takaritva`), így a már bekerültek maguktól eltűnnek. Vigyázni kell a „nyertes" szóval: a „Nyertes projektek megvalósításának támogatása" is kiesik — ez tudatos kompromisszum, ilyen kiírás ritka.
- **Csak magánszemélyes kiírások kiejtése (2026-08-12):** ha a `jogosultsag_kinyerese()` PONTOSAN `["maganszemely"]`-t ad, a tétel kiesik (`csak-maganszemely:<url>` tiltólista) — nekünk kft + egyesület kell. Ha a lista üres (nem sikerült kiolvasni) vagy más is szerepel benne, a tétel MARAD; a bizonytalanság nem ok a törlésre.
- **Teljes nullázás + új első futás (2026-08-12):** az `allapot.json`, az `oldalak.json` és a `data/pages/*` a `_to_delete/` alá került, a `docs/adatok.json` üres (`{"frissitve": …, "tetelek": {}}`). Így a következő futás ELSŐ FUTÁS: minden forrás csendben alapoz, nem nyílik Issue-áradat. Az `index.html`-ben az `ALAP_DATUM` ezért `2026-08-12` — különben a teljes alapállomány „ÚJ" jelöléssel villogna.
- **Adminisztratív oldalak kiszűrése (2026-08-12):** `adminisztrativ(cim, url)` — a kiírók oldalain rengeteg a segédoldal (GYIK, ÁSZF, „Pályázati tudnivalók", „Pályázati naptár", „Kapcsolat", „Korábbi évek támogatásai", nyomtatványok), ami átmegy a kulcsszó-szűrésen, mert van benne „pályázat". Az `ADMIN_CIM` a cím EGÉSZÉRE illeszt (`^…$`), hogy a „Felhívás – elszámolási útmutatóval" vagy a „Kapcsolatépítési pályázat" ne akadjon fenn; emellett `ADMIN_UTVONAL` végződések (`/gyik`, `/kapcsolat`, `/altalanos`…). Mind a négy gyűjtő hívja (linkek_kigyujtese, nka_tetelek hírág, palyazatok_org_tetelek, budapest_tetelek). A meglévő 43 ilyen tétel törölve (28 BGA, 7 Erasmus-menüpont, 6 NKA, 2 Norvég).
- **Kategóriák (2026-08-12):** `KATEGORIAK` — LMBTQ, Női jogok, Színház, Zene, Ifjúság / oktatás, Zöld / környezet, Film / fotó, Képzőművészet, Irodalom, Vállalkozás, plusz `Egyéb` ha egyik sem talál. **A mérvadó számítás a kiírás TELJES SZÖVEGÉBŐL megy** (dúsításkor, felülírva); a gyűjtéskor csak ideiglenes, címből vett érték kerül be (`setdefault`). Emiatt 2026-08-12-én MINDEN tételről levettük a `dusitva` jelölést, hogy a háttér-dúsítás újraszámolja (~4 futás, napi 100). A weboldalon kategóriánként PIPA (alapból mind be) + „mind" / „egyik se" gomb. Egy tétel több kategóriába is eshet. A gyűjtéskor a CÍMBŐL kapja (`setdefault`), a dúsításkor a teljes szövegből számolódik újra (az a mérvadó). A minták szándékosan szűkek: a *meleg* (melegvíz), a *trans* (transzparens) és a *szivárvány* (Szivárvány Óvoda) NEM szerepel bennük — helyettük egyértelmű kifejezések. A weboldalon kategória-szűrő (darabszámmal) és lila kategória-címke; az „Egyéb" címkét nem írjuk ki.
- **Címkézett határidő-mező (2026-08-12) — a legfontosabb javítás:** sok oldal külön adatsorban közli a határidőt, pl. a pafi.hu-n `<dt>Határidő</dt><dd>2026. 08. 25. (13 nap)</dd>`. Ez a `<dl>` azonban a `<header>`-en BELÜL van, amit a `tetel_dusitas()` `decompose()`-a kidobott — vagyis **pont a legmegbízhatóbb adatot dobtuk el**, és utána a folyószövegben találgattunk. Új `cimkezett_hatarido(soup)`: `dt/th/strong/b/span/label/div/p` elemek közül azt keresi, amelynek a szövege PONTOSAN egy határidő-címke (`HATARIDO_CIMKE`: Érvényes, Határidő, Beadási/Benyújtási/Jelentkezési határidő, deadline…), és a következő testvérből — vagy a szülő maradék szövegéből, ha „Határidő: 2026.09.30." egy elemben van — kiolvassa az első dátumot. **A teljes DOM-on fut, még a takarítás előtt**, és elsőbbsége van a kulcsszavas kereséssel szemben. Emiatt a 98 pafi.hu-tételről töröltük a `hatarido` + `dusitva` mezőt (80-nak volt folyószövegből származó, potenciálisan téves határideje), hogy a címkézett mezőből számolódjon újra.
- **Határidő-kulcsszavak bővítése (2026-08-12):** a `HATARIDO_KULCS` eredetileg csak a „határidő/benyújt/beadás/jelentkez/éjfél" szavakat ismerte, így kimaradt a gyakori magyar megfogalmazás, ahol a határidőt a mondat *ragozása* jelöli: „A pályázati anyagokat **2026. augusztus 25. kedd 13 óráig szíveskedjenek eljuttatni**…" (FITT-díj, pafi.hu). A dátum ott volt a szövegben, de egyik kulcsszó ±120 karakteres ablakába sem esett bele → határidő nélkül maradt. Bővítés: `eljuttat`, `beérkez`, `megküld`, `óráig`, `napjáig`, `határnap`, és tő-alakok (`bead` a `beadás` helyett, hogy a „beadni/beadási" is illeszkedjen). **Az ablakot szándékosan NEM tágítottuk** — ugyanezen az oldalon az elbírálás dátuma („a döntést 2026. szeptember 25-ig") 145 karakterre van a „benyújtott" szótól, tágabb ablakkal az nyerne. A javítás után a határidő nélküli, de már dúsított 117 tételről töröltük a `dusitva` jelölést, hogy a háttér-dúsítás újra megnézze őket a bővebb szótárral (kb. 2 futás).
- **Munkaajánlat-szűrés (2026-08-12):** az álláshirdetés nem pályázat, de sokszor „pályázat" néven fut (közszféra: „pályázati felhívás … munkakör betöltésére"), és a kiíró saját hírfolyamában jelenik meg (pl. `bgazrt.hu/tamogataskezelesi-szakmai-referens/`). Két réteg: (1) `munkaajanlat_cim()` — `MUNKA_CIM_RE` munkakör-megnevezésekre a címben/URL-ben, **szóhatárral**, hogy a „kertészkedés"/„Munkakörülmények"-féle összetételek ne akadjanak fenn; a `linkek_kigyujtese()` és a `jozsefvaros_tetelek()` hívja, így be sem kerülnek. (2) `munkaajanlat_szoveg()` — a kiírás szövegében legalább `MUNKA_JEL_KELL` (2) erős jel (önéletrajz, munkavégzés helye, amit kínálunk, teljes munkaidő, próbaidő, bérezés…); a `tetel_dusitas()` adja vissza 4. elemként. **Fontos:** ez a menü/lábléc NÉLKÜLI szövegre fut — a bgazrt.hu minden oldalának menüjében ott az „Álláshirdetés" szó, ezért egyetlen szó sosem elég. Felismeréskor a tétel az `allapot.json`-ba `munkaajanlat:<url>` kulccsal kerül (tiltólista), a `docs/adatok.json`-ból törlődik, és a következő futásoktól már a forrás beolvasásakor kiesik — nem töltjük le újra. A riport „Megjegyzések" alatt jelzi a darabszámot. A 3 addigi álláshirdetés (mind bgazrt.hu) törölve és tiltólistára téve.
- **Új forrás felvétele biztonságos:** az első sikeres beolvasás csendes — a tételek `allapot.json`-ba kerülnek, de nem jelennek meg találatként, és a forrás `forras-alap:<név>` markert kap. Így egy új forrás nem árasztja el az Issue-t. (Az Issue ilyenkor is nyílik, „alapállapot felvéve" címmel.)
- **Bulk-guard:** már alapozott forrásnál ha egyszerre >12 „új" jön ÉS ez a forrás találatainak >60%-a, az oldalszerkezet-változás → csendes rögzítés, riasztás helyett Megjegyzés a riportban.
- **`UJ_HATAR` cutoff (2026-07-20):** csak az ez után MEGJELENT kiírás számít valódi újdonságnak. Az új tételek cikkoldalát a radar letölti (max `DUSITAS_LIMIT`/futás), kinyeri a megjelenési dátumot + határidőt (magyar dátumminták, „határidő" kulcsszó-környezet). Régi megjelenésű / lejárt határidejű tétel csendben rögzül (a weboldalon látszik). Ha az oldal nem tölthető le → kétség esetén ÚJ. A kinyert `megjelent`/`hatarido` a `docs/adatok.json`-ba kerül, a weboldal mutatja (≤14 nap: piros).
- **Jogosultság-kinyerés:** a dúsítás a kiírás szövegéből a „pályázók köre / pályázhat / jogosult / kedvezményezett" kulcsszavak −80/+300 karakteres környezetében keres szervezettípusokat (`JOGOSULT_KATEGORIAK`: civil, vallalkozas, onkormanyzat, maganszemely) → `palyazhat` mező. A weboldal Kinek-szűrője ezt használja elsőként, és csak `palyazhat` híján esik vissza a forrás `kinek` besorolására. Csak magánszemélyes/önkormányzatos tétel egyik szűrő alatt sem jelenik meg — ez szándékos.
- **Háttér-dúsítás:** futásonként `HATTER_DUSITAS_LIMIT` (100) még nem dúsított RÉGI tétel oldalát is megnézzük (most listázottak + legfrissebbek elöl; 10 egymást követő letöltési hiba után stop) — a teljes állomány kb. egy hónap alatt ér be. A futásidő emiatt pár perccel hosszabb.
- **Watch-oldalak (`WATCH_OLDALAK`):** csak változást figyelünk (normalizált szöveg hash + diff-kivonat: mi került fel). Első látás csendes. Változásnál Issue nyílik („változás a figyelt aloldalakon"), a riportban a friss sorok. Jelenleg: BGA Városi Civil Alap, NKA miniszteri, norvegcivilalap.hu, Józsefváros hirdetőtábla.
- **A régi prototípus** (`..\palyazat-radar` mappa — sources.yaml, parsers, SQLite, sitegen) ötletforrásként megmaradt; a zajszűrés, bulk-guard, dúsítás, cutoff és watch innen lett átemelve 2026-07-22-én.
- `kultura.kreativeuropa.hu` iso-8859-2 kódolású — a `fetch()` kezeli, ne "javítsd ki".
- NKA: az oldal kollégiumonként jelzi a "(felhívás elérhető)" állapotot — ez találat akkor is, ha nem új URL (`nka-kollegium:` kulcs).
- Kulcsszó-szűrés a címre + URL-útvonalra megy, a **domain szándékosan nem számít** (különben a palyazatok.org minden linkje találat lenne).
- Értesítés e-mailben csak akkor jön, ha Attila a repónál bekapcsolta: Watch → All activity.
- A régi Cowork-os ütemezett radar ("napi-palyazat-radar") kikapcsolva; a régi állapotfájlja (`..\palyazatradar_allapot.json`, 253 tétel) a szülőmappában maradt, már nem frissül.

## Weboldal (2026-07-22 óta él)

- https://nemelvagyunk.github.io/palyazatradar/ — GitHub Pages, main ág `/docs` mappából, minden pushnál újraépül.
- Egyetlen statikus `docs/index.html` (vanilla JS, függőség nélkül), a `docs/adatok.json`-t olvassa.
- Az `adatok.json`-t a `radar.py` vezeti minden futáskor: minden LÁTOTT tételt frissít (`utolso`=ma), az `elso` az allapot.json-beli első észlelés. Cím: az első nem üres marad.
- „ÚJ" jelölés: `elso` legfeljebb 7 nappal a `frissitve` előtt. „Már nem listázott": `utolso` < `frissitve`.
- NKA-kulcsú (`nka-kollegium:`) tételek linkje a NKA kollégiumi felhívások oldalára mutat.

## Tervezett fejlesztési irányok (prioritás még nincs eldöntve)

1. ~~**Határidő kinyerése**~~ — ✅ kész (dúsítás; az összeg/keret kinyerése még nyitott)
2. **Relevancia-pontozás** — a projektprofilra (kultúrház, koncert, közösségi tér, civil, felújítás, ifjúsági) pontozni a találatokat, relevánsak előre
3. ~~**GitHub Pages weboldal**~~ — ✅ kész (lásd fent)
4. **Heti összefoglaló** — hétfőnként rendezett digest a friss + közelgő határidejű pályázatokról
5. **Napi e-mail + RSS** — a régi prototípusban kész recept van rá (Gmail app-jelszó + secrets, feed.xml), Attila egyelőre nem kérte

(Volt egy korábbi, azóta törölt prototípus — sitegen + RSS + SQLite ötletekkel; az irányok újrahasznosíthatók.)
